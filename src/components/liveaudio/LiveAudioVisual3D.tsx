import React from 'react';
import { Theme } from '../../../types';

interface LiveAudioVisual3DProps {
  inputNode: any;
  outputNode: any;
  theme: Theme;
  isRecording: boolean;
  isAiSpeaking?: boolean;
}

export const LiveAudioVisual3D: React.FC<LiveAudioVisual3DProps> = () => null;

export default LiveAudioVisual3D;